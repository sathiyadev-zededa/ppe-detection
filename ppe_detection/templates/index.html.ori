<!DOCTYPE html>
<html>
<head>
    <title>Video Stream</title>
    <style>
        body {
            margin: 0;
            background-color: #f0f0f0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: auto;
            flex-direction: column;
            padding: 20px;
        }
        h1 {
            text-align: center;
            color: #333;
        }
        .video-container {
            background-color: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0,0,0,0.2);
            margin-bottom: 30px;
        }
        .video-container img,
        .video-container video {
            width: 640px;
            height: auto;
            display: block;
        }
        a.download-link {
            margin-top: 10px;
            display: inline-block;
            background-color: #007BFF;
            color: white;
            padding: 10px 15px;
            border-radius: 5px;
            text-decoration: none;
        }
        a.download-link:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <h1>Live Inference Stream (MJPEG)</h1>
    <div class="video-container">
        <img src="{{ url_for('video_feed') }}" alt="Inference output">
    </div>

    <h1>Recorded Video (MP4)</h1>
    <div class="video-container">
        <video controls>
            <source src="{{ url_for('download_video') }}" type="video/mp4">
            Your browser does not support the video tag.
        </video>
        <a href="{{ url_for('download_video') }}" download class="download-link">Download Processed Video</a>
    </div>
</body>
</html>

